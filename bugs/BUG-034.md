# BUG-034: Функция PlacesListContent превышает лимит 50 строк

**Автор:** QA Tester
**Дата обнаружения:** 2026-03-21
**Связанная задача:** TASK-014
**Ветка/PR:** feature/TASK-014-search-and-filters / PR #16
**Критичность:** Low

---

## Описание

Функция `PlacesListContent` в файле `PlacesListScreen.kt` (строки 85–135) занимает 51 строку, нарушая правило CLAUDE.md: «Функции не длиннее 50 строк — рефакторить при превышении».

```kotlin
@Composable
internal fun PlacesListContent(          // строка 85
    uiState: PlacesListUiState,
    onNavigateToDetail: (String) -> Unit,
    onNavigateToAdd: () -> Unit,
    onDeletePlace: (String) -> Unit,
    onSearchQueryChanged: (String) -> Unit,
    onCategorySelected: (PlaceCategory?) -> Unit,
    modifier: Modifier = Modifier,
) {
    ...
}                                        // строка 135
```

Длина: 51 строка (превышение на 1 строку).

## Шаги воспроизведения

1. Открыть `PlacesListScreen.kt`.
2. Подсчитать строки функции `PlacesListContent` (85–135).
3. Результат: 51 строка.

## Ожидаемый результат

Функция не превышает 50 строк согласно стандарту кода проекта.

## Фактический результат

Функция занимает 51 строку. Превышение минимальное (1 строка), но формально нарушает стандарт.

## Среда воспроизведения

- **Файл:** `android core/app/src/main/java/com/example/localguide_planner/ui/places/list/PlacesListScreen.kt`
- **Строки:** 85–135

## Логи / Stack Trace

```
(нет — статический анализ кода)
```

## Скриншоты / Видео

Не применимо.

## Возможная причина

Добавление параметров `onSearchQueryChanged` и `onCategorySelected` в существующую функцию увеличило её длину до 51 строки.

---

## Статус бага

**Статус:** open

| Дата | Статус | Агент | Примечание |
|------|--------|-------|-----------|
| 2026-03-21 | open | QA Tester | Баг обнаружен в PR #16 |

---

## Для Developer (заполняется при фиксе)

**Корневая причина:**

**Описание фикса:**

**Файлы изменены:**
- `android core/app/src/main/java/com/example/localguide_planner/ui/places/list/PlacesListScreen.kt`

**Коммит:**
